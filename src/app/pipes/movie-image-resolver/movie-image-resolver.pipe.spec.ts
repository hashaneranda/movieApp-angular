import { MovieImageResolverPipe } from './movie-image-resolver.pipe';

describe('MovieImageResolverPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieImageResolverPipe();
    expect(pipe).toBeTruthy();
  });
});
