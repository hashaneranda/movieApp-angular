import { AddFavoriteDirective } from './add-favorite.directive';

describe('AddFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new AddFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
