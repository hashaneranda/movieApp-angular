import { WatchMovieDirective } from './watch-movie.directive';

describe('WatchMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new WatchMovieDirective();
    expect(directive).toBeTruthy();
  });
});
