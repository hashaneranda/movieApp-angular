import { WatchLaterDirective } from './watch-later.directive';

describe('WatchLaterDirective', () => {
  it('should create an instance', () => {
    const directive = new WatchLaterDirective();
    expect(directive).toBeTruthy();
  });
});
